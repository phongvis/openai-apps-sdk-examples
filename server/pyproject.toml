[project]
name = "radar-lite-server"
version = "0.1.0"
description = "Radar Lite MCP server for ChatGPT Apps SDK"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "mcp>=1.0.0",
    "uvicorn>=0.30.0",
    "httpx>=0.28.0",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.8.0",
]
